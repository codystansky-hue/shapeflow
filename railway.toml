# Railway project configuration
# Each service is configured separately via Railway dashboard
# This file provides defaults

[build]
# Nixpacks will auto-detect Node.js

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
